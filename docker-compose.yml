# docker/docker-compose.yml
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: prefect
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-prefect}
      POSTGRES_DB: prefect
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - data-network
    restart: unless-stopped

  prefect-server:
    image: prefecthq/prefect:3.4.25-python3.12
    depends_on:
      - postgres
    environment:
      PREFECT_API_DATABASE_CONNECTION_URL: postgresql+asyncpg://prefect:${POSTGRES_PASSWORD:-prefect}@postgres:5432/prefect
      PREFECT_SERVER_API_HOST: 0.0.0.0
      PREFECT_API_URL: http://prefect-server:4200/api
      PREFECT_UI_API_URL: ${SERVICE_URL_PREFECT_SERVER}/api
    ports:
      - "4200:4200"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:4200/api/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 120s
    command: prefect server start
    networks:
      - data-network
    restart: unless-stopped

  prefect-worker:
    build:
      context: .
      dockerfile: docker/Dockerfile.prefect
    depends_on:
      prefect-server:
        condition: service_healthy
    environment:
      PREFECT_API_URL: "http://prefect-server:4200/api"
      ANALYTICS_PIPELINES_DIR: "/data/pipelines"
      CLICKHOUSE_HOST: clickhouse
      CLICKHOUSE_PORT: ${CLICKHOUSE_PORT:-8123}
      CLICKHOUSE_NATIVE_PORT: ${CLICKHOUSE_NATIVE_PORT:-9000}
      CLICKHOUSE_DB: ${CLICKHOUSE_DB:-default}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER:-default}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD}
      LIGHTDASH_INTERNAL_URL: "http://lightdash:8080"
      LIGHTDASH_API_KEY: ${LIGHTDASH_API_KEY}
      GIT_AUTHOR_NAME: "Lightdash Sync"
      GIT_AUTHOR_EMAIL: "sync@data-platform"
      GIT_COMMITTER_NAME: "Lightdash Sync"
      GIT_COMMITTER_EMAIL: "sync@data-platform"
    volumes:
      - ~/data:/data
      - ./dbt:/opt/prefect/dbt  # rw: sync flow writes downloaded content
      - .:/repo                 # full repo root for git operations
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - data-network
    restart: unless-stopped

  clickhouse:
    image: clickhouse/clickhouse-server:24.3
    environment:
      CLICKHOUSE_DB: ${CLICKHOUSE_DB:-default}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER:-default}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-changeme}
      # Readonly user password — loaded by users.d/bi_readonly.xml at startup
      CLICKHOUSE_READONLY_PASSWORD: ${CLICKHOUSE_READONLY_PASSWORD}
    mem_limit: 2g
    volumes:
      - ~/data/clickhouse:/var/lib/clickhouse
      # File-level mount: injects our user without shadowing default-user.xml
      - ./clickhouse/users.d/bi_readonly.xml:/etc/clickhouse-server/users.d/bi_readonly.xml
    ports:
      - "8123:8123"
      - "9000:9000"
    networks:
      - data-network
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    volumes:
      - ~/data/minio:/data
    ports:
      - "9001:9001"   # console UI
    command: server /data --console-address ":9001"
    networks:
      - data-network
    restart: unless-stopped

  minio-init:
    image: minio/mc:latest
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
        sleep 5 &&
        mc alias set local http://minio:9000 $${MINIO_ROOT_USER:-minioadmin} $${MINIO_ROOT_PASSWORD:-minioadmin} &&
        mc mb --ignore-existing local/lightdash
      "
    networks:
      - data-network
    restart: "no"

  lightdash-db:
    image: postgres:15
    environment:
      POSTGRES_USER: lightdash
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-prefect}
      POSTGRES_DB: lightdash
    volumes:
      - lightdash_db_data:/var/lib/postgresql/data
    networks:
      - data-network
    restart: unless-stopped

  lightdash:
    image: lightdash/lightdash:0.2473.1   # pinned to match CLI version
    platform: linux/amd64                 # amd64-only image, runs via Rosetta on Mac
    depends_on:
      lightdash-db:
        condition: service_started
      clickhouse:
        condition: service_started
    environment:
      - PGHOST=lightdash-db
      - PGPORT=5432
      - PGUSER=lightdash
      - PGPASSWORD=${POSTGRES_PASSWORD:-prefect}
      - PGDATABASE=lightdash
      - LIGHTDASH_SECRET=${LIGHTDASH_SECRET:-changeme}
      - LIGHTDASH_SECURE_COOKIES=false
      - DBT_PROJECT_DIR=/usr/app/dbt
      - S3_ENDPOINT=http://minio:9000
      - S3_BUCKET=lightdash
      - S3_REGION=us-east-1
      - S3_ACCESS_KEY=${MINIO_ROOT_USER:-minioadmin}
      - S3_SECRET_KEY=${MINIO_ROOT_PASSWORD:-minioadmin}
      - S3_FORCE_PATH_STYLE=true
      - NODE_OPTIONS=--max-old-space-size=640
    volumes:
      - ./dbt:/usr/app/dbt
    networks:
      - data-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8080/api/v1/health', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s

  # Runs dlt ingestion + dbt transformation on every deployment
  pipeline-init:
    build:
      context: .
      dockerfile: docker/Dockerfile.prefect
    depends_on:
      prefect-server:
        condition: service_healthy
      clickhouse:
        condition: service_started
    environment:
      PREFECT_API_URL: "http://prefect-server:4200/api"
      ANALYTICS_PIPELINES_DIR: "/data/pipelines"
      CLICKHOUSE_HOST: clickhouse
      CLICKHOUSE_PORT: ${CLICKHOUSE_PORT:-8123}
      CLICKHOUSE_NATIVE_PORT: ${CLICKHOUSE_NATIVE_PORT:-9000}
      CLICKHOUSE_DB: ${CLICKHOUSE_DB:-default}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER:-default}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD}
    volumes:
      - ~/data:/data
      - ./dbt:/opt/prefect/dbt:ro
    command: python /opt/prefect/prefect/flows/main_pipeline.py
    networks:
      - data-network
    restart: "no"

  # One-time manual first-boot (free self-hosted Lightdash limitation):
  #   1. Visit http://localhost:8080/register and create your admin account
  #   2. Go to Settings → Personal Access Tokens → generate a token
  #   3. Set LIGHTDASH_API_KEY in .env (local) or Coolify env vars (VPS)
  #   4. Run: docker-compose up lightdash-deploy
  # On subsequent deploys (e.g. after dbt model changes), just re-run step 4.
  lightdash-deploy:
    build:
      context: .
      dockerfile: docker/Dockerfile.lightdash-deploy
    working_dir: /dbt
    depends_on:
      lightdash:
        condition: service_healthy
    environment:
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=${CLICKHOUSE_PORT:-8123}
      # Readonly user: stored in Lightdash's DB and used by the web app for all queries
      - CLICKHOUSE_USER=bi_readonly
      - CLICKHOUSE_PASSWORD=${CLICKHOUSE_READONLY_PASSWORD}
      - LIGHTDASH_URL=http://lightdash:8080
      - LIGHTDASH_EMAIL=${LIGHTDASH_EMAIL}
      - LIGHTDASH_PASSWORD=${LIGHTDASH_PASSWORD}
      - LIGHTDASH_API_KEY=${LIGHTDASH_API_KEY}
      - CI=true
    volumes:
      - ./dbt:/dbt
      - ./dbt/profiles.yml:/root/.dbt/profiles.yml
    networks:
      - data-network
    restart: "no"

  vanna:
    build:
      context: .
      dockerfile: docker/Dockerfile.vanna
    environment:
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=${CLICKHOUSE_PORT:-8123}
      # Readonly user: agent can only run SELECT queries
      - CLICKHOUSE_USER=bi_readonly
      - CLICKHOUSE_PASSWORD=${CLICKHOUSE_READONLY_PASSWORD}
      - LIGHTDASH_PUBLIC_URL=${LIGHTDASH_PUBLIC_URL:-http://localhost:8080}
      - LIGHTDASH_INTERNAL_URL=http://lightdash:8080
      - LIGHTDASH_API_KEY=${LIGHTDASH_API_KEY}
      - LIGHTDASH_EMAIL=${LIGHTDASH_EMAIL}
      - LIGHTDASH_PASSWORD=${LIGHTDASH_PASSWORD}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - VANNA_MODEL=${VANNA_MODEL:-deepseek-chat}
      - CHROMADB_PATH=/data/vanna-chromadb
    volumes:
      - ~/data:/data
      - ./dbt:/dbt
      - chroma_cache:/root/.cache/chroma
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "8084:8084"
    networks:
      - data-network
    restart: unless-stopped

  # Runs smoke test after pipeline + lightdash-deploy complete
  smoke-test:
    build:
      context: .
      dockerfile: docker/Dockerfile.vanna
    depends_on:
      pipeline-init:
        condition: service_completed_successfully
      lightdash-deploy:
        condition: service_completed_successfully
      vanna:
        condition: service_started
    command: python /app/smoke_test.py --url http://vanna:8084
    networks:
      - data-network
    restart: "no"

  # nginx sits in front of Lightdash and injects the Vanna chat widget.
  # Lightdash UI is accessed via http://localhost:8080 (nginx port).
  nginx:
    image: nginx:alpine
    depends_on:
      lightdash:
        condition: service_healthy
      vanna:
        condition: service_started
    volumes:
      - ./nginx/lightdash.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8080:80"
    networks:
      - data-network
    restart: unless-stopped

volumes:
  postgres_data:
  lightdash_db_data:
  chroma_cache:

networks:
  data-network:
    driver: bridge