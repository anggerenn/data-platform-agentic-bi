FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    "vanna[chromadb,openai]==0.7.9" \
    "pydantic-ai[openai]" \
    clickhouse-connect \
    python-dotenv \
    flask

COPY vanna/ .

EXPOSE 8084

CMD ["python", "app.py"]
