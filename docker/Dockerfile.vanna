FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    "pydantic-ai[openai]" \
    clickhouse-connect \
    rank-bm25 \
    pandas \
    python-dotenv \
    flask \
    docker \
    pyyaml

COPY vanna/ .

EXPOSE 8084

CMD ["python", "app.py"]
